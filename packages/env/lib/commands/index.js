/**
 * Internal dependencies
 */
const start = require( './start' );
const stop = require( './stop' );
const clean = require( './clean' );
const run = require( './run' );
const phpunit = require( './phpunit' );

module.exports = {
	start,
	stop,
	clean,
	run,
	phpunit,
};
